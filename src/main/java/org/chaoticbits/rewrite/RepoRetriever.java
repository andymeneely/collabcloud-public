package org.chaoticbits.rewrite;

/**
 * The class that retrieves the repositories from the 
 * given time frame
 * 
 * @author Marissa Wilson
 *
 */
public class RepoRetriever {

	/*
	 * Is given a time frame
	 * Collects every repo from that time frame
	 * Returns the repos, format still uncertain
	 */
	public static file retrieve(){
		
	}
}
