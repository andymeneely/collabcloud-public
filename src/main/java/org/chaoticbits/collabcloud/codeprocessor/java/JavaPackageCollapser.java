package org.chaoticbits.collabcloud.codeprocessor.java;

import org.chaoticbits.collabcloud.CloudWeights;

public class JavaPackageCollapser {

	public CloudWeights collapse(CloudWeights weights) {
//		Set<ISummaryToken> pkgs = getPkgs(weights);
//		String str = "abc";
//		
//		for (Entry<ISummaryToken, Double> entry : entries) {
//			
//		}
//		CloudWeights newWeights = new CloudWeights();
//		
//		return newWeights;
		
		//Stopped development on this - seems a little slow for what it does
		//Keeping it anyway just in case
		throw new IllegalStateException("unimplemented!");
	}

//	private Set<ISummaryToken> getPkgs(CloudWeights weights) {
//		Set<ISummaryToken> tokens = weights.tokens();
//		Set<ISummaryToken> pkgs = new LinkedHashSet<ISummaryToken>();
//		for (ISummaryToken token : tokens) {
//			if(token.getType() == JavaTokenType.PACKAGE)
//				pkgs.add(token);
//		}
//		return pkgs;
//	}

}
