package org.chaoticbits.collabcloud.vc.git;

import org.junit.Test;

public class GitContributionNetworkTest {

	@Test
	public void simpleContributionNetwork() throws Exception {
//		IMocksControl ctrl = createControl();
		// @SuppressWarnings("unchecked")
		// // RevCommit commit1 = RevCommit.fromString("");
		// // RevCommit commit2 = new RevCommit(null);
		// // RevCommit commit3 = new RevCommit(null);
		// Iterator<RevCommit> itr = (Iterator<RevCommit>) ctrl.createMock(Iterator.class);
		// expect(itr.hasNext()).andReturn(true).times(2);
		// // expect(itr.next()).andReturn(commit1).once();
		// // expect(itr.next()).andReturn(commit2).once();
		// // expect(itr.next()).andReturn(commit3).once();
		// expect(itr.hasNext()).andReturn(false).once();
		// A
		// |
		// andy--B--bob
//		Developer andy = new Developer("Andy", "andy.meneely@gmail.com");
//		Developer bob = new Developer("Bob", "bob");
//		GitLoader gitLoader = new GitLoader(new File("testgitrepo/.git"), GitLoaderTest.SECOND_COMMIT_ID);
//
//		ctrl.replay();
//		Graph<ISummaryToken, Long> g = gitLoader.contributionNetwork();
//		assertTrue(g.containsVertex(andy));
//		assertTrue(g.containsVertex(bob));
//		assertTrue(g.containsVertex(andy));
//		ctrl.verify();
	}
}
